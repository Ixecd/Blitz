apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: web3-blitz-etcd
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      app: web3-blitz-etcd
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: web3-blitz
      ports:
        - port: 2379
          protocol: TCP
