apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web3-blitz.fullname" . }}-test-connection"
  labels:
    {{- include "web3-blitz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web3-blitz.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
