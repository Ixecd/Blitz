version: "2"
sql:
  - engine: "postgresql"
    queries: "queries.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "."
        emit_db_tags: true
        emit_json_tags: true
        emit_interface: true
        emit_prepared_queries: false